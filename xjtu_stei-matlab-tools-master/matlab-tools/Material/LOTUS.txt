[4 Feb. 2021]

一直用 XJTU_STEI 账户也不太好, 毕竟是公用账户... 又建了一个私人账户, 添加为仓库管理员. 这样正常多了.

git commit 并不能对每个文件单独指定 commit message, 这似乎暗示 commit 是针对整个项目的版本迭代(而非如本例中针对单个文件的子项目).

遇到了奇怪的 git 问题... --local 并没有 override --global 设置?

我刚才是 XJTU_STEI, 但我现在是 adqeor.

[2 Feb. 2021]

连续好几天改历史遗留的 MATLAB 代码, 简直了... 之前的绘图风格丑到没法看. 用 15b 进行绘图的时候疯狂用 gca 之类的怪指令.

WSL2 Ubuntu 上面的 oh-my-zsh 更改主题有 bug . 比如说, 设置为 bureau 之类的, 有两行提示符的, 再切换为其他主题时, 出现切换失败. 要不就是上面一行阴魂不散, 要不就是直接切不过去...

